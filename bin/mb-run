#!/usr/bin/env node
import { getErrorCode, getErrorMessage, main } from "../dist/module.js";

try {
  await main();
} catch (error) {
  // oxlint-disable-next-line no-console
  console.error(getErrorMessage(error));
  process.exitCode = getErrorCode(error);
}
